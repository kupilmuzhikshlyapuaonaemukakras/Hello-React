import "./MyAccount.css"
import { Button } from '@material-ui/core';

export default function MyAccountButton() {
    return (
        <div className="MyAccountButton">
            <Button size="large" color="inherit">My Account</Button>
        </div>
    );
}